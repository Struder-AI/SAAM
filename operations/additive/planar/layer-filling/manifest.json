{
  "manifestSchemaVersion": 1,
  "id": "layer-filling",
  "version": "0.1.0",
  "name": "Planar layer filling",
  "summary": "Rectangular and circular/annular planar coverage: perimeters, plus alternating-direction raster fill clipped to whichever boundary shape was requested.",
  "category": ["additive", "planar"],
  "capabilityRequirements": ["planar-motion", "extrusion-on-off"],
  "maturity": "experimental",
  "evidence": {
    "label": "EXPERIMENTAL",
    "scope": "Deterministic generator validated by schema, geometry-bounds, and golden-output tests. Not yet evidenced on physical hardware under this exact manifest and generator revision."
  },
  "inputs": {
    "type": "object",
    "properties": {
      "geometry": { "type": "string", "enum": ["rectangle", "circle", "annulus"] },
      "width": { "type": "number", "exclusiveMinimum": 0 },
      "depth": { "type": "number", "exclusiveMinimum": 0 },
      "outerDiameter": { "type": "number", "exclusiveMinimum": 0 },
      "innerDiameter": { "type": "number", "minimum": 0 },
      "layers": { "type": "integer", "minimum": 1 },
      "wallCount": { "type": "integer", "minimum": 1 },
      "zStart": { "type": "number" },
      "infillDensity": { "type": "number", "exclusiveMinimum": 0, "maximum": 1, "description": "Rectangular geometry only. Approximate single-layer fill coverage fraction (bead width / line spacing) — a rough rectilinear-infill density model, not evidence specific to this project. Omit to keep the previous behavior of filling at settings.spacing directly." },
      "solidBottomLayers": { "type": "integer", "minimum": 0, "description": "Rectangular geometry only. This many layers from the bottom are filled at full density (spacing == beadWidth) regardless of infillDensity." },
      "solidTopLayers": { "type": "integer", "minimum": 0, "description": "Rectangular geometry only. This many layers from the top are filled at full density regardless of infillDensity." },
      "boreDiameter": { "type": "number", "exclusiveMinimum": 0, "description": "Rectangular geometry only. A centered cylindrical bore into the top face — distinct from innerDiameter's full-through annulus, which only applies to circular/annular geometry. Requires boreDepth." },
      "boreDepth": { "type": "number", "exclusiveMinimum": 0, "description": "Depth of boreDiameter's bore, measured down from the top face. Cuts through walls, the bore's own inner-perimeter walls, and fill alike on every affected layer, regardless of that layer's solid/sparse classification — a bore open at the top face has to stay open through any solid top layers too. For a blind hole (boreDepth less than the part's full height), the solidTopLayers count of layers immediately below the floor is automatically forced solid too, since the floor is a top-facing surface exactly like the part's own outer top." }
    },
    "description": "A request with a finite `outerDiameter`/`diameter`, or `geometry` set to \"circle\"/\"annulus\", resolves to a circular/annular perimeter with raster fill clipped to that boundary. Otherwise it resolves to a rectangular perimeter with raster fill clipped to `width`/`depth`, optionally with a centered bore and/or solid top/bottom layers. Either way the raster alternates direction by layer — see README.md."
  },
  "outputs": {
    "partShapes": ["box", "cylinder", "ring"],
    "pathFamilies": [
      "Outer perimeter",
      "Inner perimeter",
      "Prioritized perimeter",
      "Region-first raster"
    ]
  },
  "dependencies": [],
  "generator": {
    "module": "./generator.mjs",
    "export": "generate",
    "deterministic": true
  },
  "validation": [
    { "rule": "bounds-within-part-envelope", "description": "Every emitted point stays within the declared part envelope." },
    { "rule": "layer-count-matches-request", "description": "The number of distinct layer indices equals the requested layer count." },
    { "rule": "closed-contours", "description": "Every perimeter/ring/fill-ring path's first and last point coincide." }
  ],
  "examples": ["examples/rectilinear-box.json", "examples/concentric-ring.json"],
  "license": "Apache-2.0"
}
