{
  "name": "saam-trace-player",
  "version": "0.1.0",
  "private": true,
  "description": "Animated previewer for a SAAM motion trace: replays what a machine was told to do, on its own clock, before it runs.",
  "type": "module",
  "main": "player.mjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:standalone": "node build-standalone.mjs"
  },
  "license": "Apache-2.0"
}
