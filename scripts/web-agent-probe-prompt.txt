Run a small experiment to see whether you can run SAAM Studio for a maker from this web chat's own temporary environment. This is a connectivity test, not a request to make a part or develop/publish SAAM.

Use the attached web-agent-probe.mjs unchanged. If it is not attached but the repo is available, it is scripts/web-agent-probe.mjs. The script needs Node.js 22+ and no packages. Do not claim a different HTML artifact is this running server.

1. Identify this product/surface and the actual available tools. Check node --version. If code execution or Node is unavailable, report that exact boundary. Do not silently switch products or rewrite the probe in another language.
2. Start: node web-agent-probe.mjs serve --host 0.0.0.0 --port 4321
   Use the platform's supported background-process mechanism so it can survive the end of your response. Adapt the port if the platform requires one. Record the printed instanceId, baseUrl, controlToken and fixtureSha256. Keep the control token in your execution environment.
3. Run: node web-agent-probe.mjs status --url SESSION_URL
   This establishes only that your shell can reach the server.
4. Attempt to open the actual server in the platform's native live preview/forwarded-port UI or cloud browser. If using a forwarded origin, append the full random session path from baseUrl, including the trailing slash. If the cloud browser has a separate network, report that fact. Do not invent a preview URL. Do not publish/deploy the application, create an external tunnel or relay, install an extension, or use a local companion on my computer. We are testing capabilities already provided by this agent platform.
5. Show me the interactive page through the supported preview or browser takeover. Ask me to invent a phrase in the page, press Send, then return to chat and say only "sent". Do not click Send, enter a phrase, or call the input endpoint yourself. A screenshot or an agent-only browser view is not maker access. End your response to let me interact; do not keep this turn alive by polling.

After I say "sent":
6. Use the status command to read my phrase from the server and tell me the phrase and instanceId. Compare the ID with the original. If the process died, report the failure before restarting; do not conceal a restart.
7. Run: node web-agent-probe.mjs update --url SESSION_URL --control CONTROL_TOKEN --message "YOUR NEW DISTINCTIVE MESSAGE"
   Tell me to verify that the message appears on the page, then download the test receipt and confirm it reached my device. The receipt must contain the same instanceId. You may verify a separately downloaded copy in your environment, but that does not prove delivery to my device. The fixtureSha256 records the expected bytes.
8. End the response again. Let me wait at least two minutes before I say "resume". On resume, read status and report whether the SAME process/session, phrase and agent message remain. Record the actual elapsed time; don't infer survival from conversation history.

Report each capability separately as pass / fail / untested:
- Node server starts.
- Agent shell reaches it.
- Agent cloud browser reaches that same server (or unavailable).
- Maker can interact with it, stating native preview vs browser takeover.
- Maker-entered phrase reaches agent through server state.
- Agent update reaches maker without manual file transfer.
- Maker confirms receipt download; distinguish checksum verification if performed.
- Same session survives completed responses and the measured idle interval.

Do not mark maker steps passed without my explicit report. Endpoint logs don't authenticate a human. If any step is blocked, describe the missing capability and stop there; don't substitute a mock, static artifact or different hosting product. Leave the session running for my tests until I ask you to stop. Nothing in this probe authorizes manufacturing or creates real job approvals.
